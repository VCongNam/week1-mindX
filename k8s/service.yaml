apiVersion: v1
kind: Service
metadata:
  name: mindx-api-service
  labels:
    app: mindx-api
spec:
  type: ClusterIP
  selector:
    app: mindx-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
